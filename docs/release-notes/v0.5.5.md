【0.5.5】
修复 AI 设置「引擎 → 段总结 / 描述参数」里上下文等下拉框选了默认档位（如 8K）后被全局值顶回去的问题；修复「设置 → 关于 → 应用更新检查频率」下拉菜单被折叠面板裁切显示不全的问题。

【0.5.5】
Fixed the AI Settings → Engine → Summary / Describe pickers so picking the default tier (e.g. 8K context) actually sticks instead of being overridden by the global value. Fixed the Settings → About → Update check frequency dropdown being clipped by the collapsible panel.
