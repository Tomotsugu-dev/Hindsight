<!-- zh -->
【0.8.23】
- ⚠️ 多设备用户请注意：请将所有设备上的应用更新至本版本或更高版本。后续版本的应用分类同步将不再兼容旧版本，未更新的设备将无法接收分类变更。
- 清除某个应用的历史数据（使用时长、截图和文字索引）时，处理速度提升至原来的 10 倍以上。
- 「应用分类，多设备设置」里，组内第一个应用（组名的来源）不再显示「从该组移出」按钮；之前点它会把整组拆散。要移动它请拖到别的组
- 内置分类新增繁体中文、西班牙语、葡萄牙语系统下的常用应用名（如「記事本」「Bloc de notas」），这些系统上首次出现的应用会自动归类

<!-- tw -->
【0.8.23】
- ⚠️ 多裝置使用者請注意：請將所有裝置上的應用更新至本版本或更高版本。後續版本的應用分類同步將不再相容舊版本，未更新的裝置將無法接收分類變更。
- 清除某個應用的歷史資料（使用時間、截圖和文字索引）時，處理速度提升至原來的 10 倍以上。
- 應用配對裡，組內第一個應用（組名的來源）不再顯示「從該組移出」按鈕；之前點它會把整組拆散。要移動它請拖到別的組
- 內建分類新增繁體中文、西班牙文、葡萄牙文系統下的常用應用名稱（如「記事本」「Bloc de notas」），這些系統上首次出現的應用會自動歸類

<!-- en -->
【0.8.23】
- ⚠️ If you use several devices: please update the app on all of them to this version or later. In upcoming versions, app category sync will no longer be compatible with older versions, and devices that have not updated will not receive category changes.
- Clearing an app's historical data (usage time, screenshots and text index) is now at least 10 times as fast.
- In "App pairing across devices", the first app of a group (the one the group is named after) no longer shows the "Remove from this group" button; removing it used to break up the whole group. Drag it to another group instead
- Built-in categories now know common app names on Traditional Chinese, Spanish and Portuguese systems (for example 記事本, Bloc de notas), so those apps are categorised the first time they appear

<!-- ja -->
【0.8.23】
- ⚠️ 複数のデバイスをお使いの方へ：すべてのデバイスのアプリを本バージョン以降に更新してください。今後のバージョンでは、アプリのカテゴリ同期は旧バージョンと互換性がなくなり、更新していないデバイスはカテゴリの変更を受け取れなくなります。
- アプリの履歴データ（使用時間、スクリーンショット、テキストインデックス）を削除する際の処理速度が、従来の 10 倍以上になりました。
- アプリのペアリングで、グループの最初のアプリ（グループ名の元になっているもの）には「このグループから外す」ボタンを表示しなくなりました。以前はこれを押すとグループ全体が解散していました。移動したい場合は別のグループへドラッグしてください
- 内蔵カテゴリに繁体字中国語・スペイン語・ポルトガル語のシステムでよく使われるアプリ名（記事本、Bloc de notas など）を追加しました。これらのシステムで初めて現れたアプリは自動的に分類されます

<!-- pt -->
【0.8.23】
- ⚠️ Se você usa vários dispositivos: atualize o app em todos eles para esta versão ou uma posterior. Nas próximas versões, a sincronização de categorias de apps deixará de ser compatível com versões antigas, e os dispositivos não atualizados não receberão as mudanças de categoria.
- A limpeza dos dados históricos de um app (tempo de uso, capturas de tela e índice de texto) agora é pelo menos 10 vezes mais rápida.
- No pareamento de apps, o primeiro app de um grupo (o que dá nome ao grupo) não mostra mais o botão "Remover deste grupo"; removê-lo desfazia o grupo inteiro. Para movê-lo, arraste-o para outro grupo
- As categorias internas agora reconhecem nomes comuns de apps em sistemas em chinês tradicional, espanhol e português (por exemplo 記事本, Bloc de notas), então esses apps são categorizados na primeira vez que aparecem

<!-- es -->
【0.8.23】
- ⚠️ Si usas varios dispositivos: actualiza la app en todos ellos a esta versión o una posterior. En las próximas versiones, la sincronización de categorías de apps dejará de ser compatible con versiones antiguas, y los dispositivos no actualizados no recibirán los cambios de categoría.
- La eliminación de los datos históricos de una app (tiempo de uso, capturas de pantalla e índice de texto) ahora es al menos 10 veces más rápida.
- En el emparejamiento de apps, la primera app de un grupo (la que da nombre al grupo) ya no muestra el botón «Quitar de este grupo»; quitarla deshacía todo el grupo. Para moverla, arrástrala a otro grupo
- Las categorías integradas ahora reconocen nombres habituales de apps en sistemas en chino tradicional, español y portugués (por ejemplo 記事本, Bloc de notas), así que esas apps se clasifican la primera vez que aparecen
