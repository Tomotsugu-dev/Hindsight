【0.6.6】
- 截图功能默认关闭——升级到 v0.6.6 后所有用户的截图开关都会重置成关闭状态，要继续用截图请去「设置 → 通用 → 启用截图」手动打开。
- 工程规范打磨：日志卫生、FFI SAFETY 注释、前端页面按需加载（首屏更轻），用户视角无变化。

【0.6.6】
- Screenshot capture now defaults to OFF. Upgrading to v0.6.6 resets the screenshot toggle to OFF for every user; to re-enable it, go to Settings → General → Enable screenshots.
- Engineering hygiene polish: log discipline, FFI SAFETY annotations, lazy-loaded frontend pages (lighter initial bundle). No user-visible changes from this.
