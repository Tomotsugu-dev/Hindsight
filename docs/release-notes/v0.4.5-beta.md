【0.4.5-beta】
AI 设置·引擎参数拆成"图描述"和"段总结"两套：跑日报时引擎自动按阶段切换参数，图描述阶段拉满并发（slots），段总结阶段拉满上下文（ctx）。新增系统 VRAM 探测 + 一键"应用推荐"按钮，根据当前显卡和模型给出最优组合。

【0.4.5-beta】
AI Settings → Engine params now split into Image describe / Segment summary, each with its own picker row. The engine restarts between phases so step 1 can run with high parallel slots and step 2 with high context. New system VRAM probe + one-click "Apply recommended" button picks the best combo for your GPU and current model.
