【0.6.5】
修复 macOS 上对访达 (Finder) 截图变成纯黑画面的问题——之前 SCK 把 Finder 桌面图标窗口当成了"最大窗口"选中，又被 Apple 的 desktop-independent filter 排掉，渲染必黑。

【0.6.5】
Fixed pure-black screenshots when Finder was the focused app on macOS — SCK was picking Finder's full-screen desktop-icon window as "largest", which Apple's desktop-independent content filter then renders empty.
