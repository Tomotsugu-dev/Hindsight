【0.6.0】
- 修复了 macOS 多屏幕下焦点采集错把所有时间记到 Hindsight 自身的问题。
- 修复了 macOS 多屏幕下截图截到 Hindsight 自己窗口的问题（找不到目标窗口时回退到主显示器整屏）。
- 默认时段标签按系统语言初始化（英语 / 日语用户首装不再看到中文 "深夜 / 上午 / 下午"）。
- 修复云端同步页首次打开时按钮蓝→灰一闪的问题，改成同尺寸 skeleton 占位。
- 周统计切周时柱子去掉滑动动画，直接 snap。

【0.6.0】
- Fixed focus tracking on macOS multi-monitor setups attributing all activity to Hindsight itself.
- Fixed screenshots on macOS multi-monitor setups capturing Hindsight's own window — now falls back to capturing the primary display when the focused window can't be located.
- Default segment labels now follow the system locale (English / Japanese users on first install no longer see Chinese labels like 深夜 / 上午 / 下午).
- Fixed the blue-to-gray button flash when first opening the Cloud Sync page — replaced with a same-sized skeleton placeholder.
- Removed the slide animation when switching weeks on the Weekly Stats chart; bars now snap instantly.
