【0.6.1】
- 修复了 macOS 多屏幕下截图截到错误屏幕（默认主屏，不是你正在工作的那块屏）的问题。
- macOS 录屏权限重确认窗口期间静默跳过采集，避免每次都触发系统弹框。

【0.6.1】
- Fixed screenshots on macOS multi-monitor setups capturing the wrong screen (the user-configured primary, not the one you're actually working on).
- Silently skip capture during macOS Screen Recording re-confirmation windows to stop the system permission dialog from popping every launch.
