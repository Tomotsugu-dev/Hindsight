【0.4.4-beta】
长页面（AI 总结 / 设置 / AI 设置）滚动后顶部 tab 现在会重新浮回视口顶端，不用回滚也能切 tab。

【0.4.4-beta】
On long pages (AI Summary, Settings, AI Settings) the tab strip now floats back to the top of the viewport once you scroll past it, so switching tabs no longer requires scrolling all the way up.
