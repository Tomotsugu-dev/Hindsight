【0.7.2】
- 新增「隐藏」分类：拖进去的应用不参与统计、排行、AI 总结。
- 「应用」页加工具栏：搜索 + 分类筛选 + 排序，状态记住。
- 侧边栏重构：「分类 / 应用」拆两页；「今日总览」→「日统计」；分组 header 化。
- 最常用应用 / 分类列表默认前 10，点 chevron 展开全部。
- 修复 macOS：分类页应用列表展开动画从"闪现"恢复到平滑。
- 视觉细节：分类指派图标化；色板扩到 21 色；编辑 / 删除按钮 hover 才染色。

【0.7.2】
- New "Hidden" category — apps dragged in disappear from stats, rankings, and AI summaries.
- Apps page toolbar: search + category filter + sort; state persists across reloads.
- Sidebar overhaul: Categories / Apps split into two entries; "Today" renamed "Daily"; explicit group headers.
- Top-apps / top-categories lists show 10 by default; chevron expands to all.
- Fix on macOS: Categories page's app-list expand animation, used to flash instantly, now smooth.
- Visual polish: assign-category uses category icons; 21-color palette; edit / delete buttons go neutral by default.
