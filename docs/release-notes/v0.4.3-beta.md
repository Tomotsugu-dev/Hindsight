【0.4.3-beta】
调试 tab UI 重做，新增"仅生成图片描述 / 仅生成段落总结"两个独立按钮，方便分别调试两步生成。

【0.4.3-beta】
Reworked Debug tab UI; added separate "image-description only" and "paragraph-summary only" buttons so each generation step can be tested in isolation.
