<!-- zh -->
【0.8.10】
- 屏幕记忆索引的「停止」按钮现在也能停止常驻 OCR 的后台批次，不再只对手动回填有效；要彻底停止请关闭「常驻 OCR」。
- 开发版（tauri dev）不再改写开机自启动路径，避免重启后拉起无法加载界面的 debug 构建。


<!-- tw -->
【0.8.10】
- 螢幕記憶索引的「停止」按鈕現在也能停止常駐 OCR 的背景批次，不再只對手動回填有效；要徹底停止請關閉「常駐 OCR」。
- 開發版（tauri dev）不再改寫開機自啟動路徑，避免重新開機後拉起無法載入介面的 debug 建置。


<!-- en -->
【0.8.10】
- The screen-memory indexing Stop button now also stops Resident OCR's background batches, not just manual backfills; turn off Resident OCR to stop for good.
- The development build (tauri dev) no longer rewrites the autostart path, so a reboot won't launch a debug build that can't load its UI.


<!-- ja -->
【0.8.10】
- 画面メモリのインデックス「停止」ボタンが、常駐 OCR のバックグラウンドバッチも停止できるようになりました（従来は手動取り込みのみ）。完全に止めるには「常駐 OCR」をオフにしてください。
- 開発版（tauri dev）が自動起動のパスを書き換えなくなり、再起動後に UI を読み込めない debug ビルドが立ち上がる問題を防ぎます。


<!-- pt -->
【0.8.10】
- O botão Parar da indexação da memória de tela agora também interrompe os lotes em segundo plano do OCR residente, não apenas o preenchimento manual; para parar de vez, desative o OCR residente.
- A versão de desenvolvimento (tauri dev) não sobrescreve mais o caminho de inicialização automática, evitando que o Windows abra um build de debug sem interface após reiniciar.
