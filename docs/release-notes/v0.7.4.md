【0.7.4】
- 占比视图加 3 个数据 tile：vs 昨日 / 上周 / 上月、峰值时点（含当时使用时长）、主力大类；点饼图切片直接 pin 高亮，下方排行 + tile 一起切到该大类视角，tile 左侧染该大类色。
- 大类卡（把多个小分类聚成一组的顶层容器，例如「工作」装编程 / 办公 / 工作沟通）：删除按钮改成右上角 × 小按钮，hover 卡才出；名字加 I-beam 光标提示可双击改名。
- AI 设置 - 模型：去掉「✓ 已下载」冗余 badge，靠 step toggle + 卸载就能看出装没装。


【0.7.4】
- Pie view gets a 3-tile insights row: vs yesterday / last week / last month, peak time (with duration), dominant super-category. Clicking a slice pins the highlight, and the ranking cards + tiles all scope to that super-category, with the tiles taking on its accent color.
- Super-category cards (the top-level buckets that group related categories — e.g. "Work" contains Code / Office / Work Chat): delete is now a small × at the top-right corner, revealed on hover; names show an I-beam cursor as a rename hint.
- AI Settings → Models: drop the redundant "✓ Downloaded" badge on installed models — step toggles plus the uninstall button already make state obvious.
