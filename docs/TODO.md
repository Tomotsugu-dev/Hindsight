# TODO

## 2026-06-29

1. MacOS 端希望点击 'x' 按钮后，能够直接缩小到右上角，而不是最小化到 Dock 中。
2. MacOS 的通用：登录与扩展项中显示的是 '开发者真实姓名'，应该改为应用名字。

## 2026-07-04

1. 已知缺陷：截图功能关闭时，被动观看（看视频/盯编译等零键鼠输入场景）的使用时长不被统计——
   键鼠空闲 3 分钟即封存会话，时长记到最后一次键鼠输入为止。
   原因：挂机判定的第二信号"屏幕活跃探测"（`capture/service.rs::screen_probe_active`）
   在 `screenshot_enabled = false` 时停用，以严格兑现"关截图 = 完全不读屏"的隐私语义。
   探测本身只在内存比较 256×144 缩略图、不落盘，技术上可以放开（删掉函数开头的
   enabled 检查即可），但这会改变隐私开关的语义，需要产品决策：
   时长统计准确性 vs 隐私承诺严格性。截图开启时无此问题（2026-07-04 已修复被动观看断供）。
