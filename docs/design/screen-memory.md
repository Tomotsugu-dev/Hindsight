# 屏幕记忆(Screen Memory)架构设计

状态(2026-07-05):L2/L3 已落地(存档规格切换 + memory.sqlite + PP-OCRv5 消化管线 + 行级折叠 + FTS,真实档案 e2e 通过);Chat 已落地(guarded agent loop + 三工具 + 证据卡 + 会话历史落库,`src-tauri/src/chat/`);旧日报管线(step1 VLM + MobileNet 去重)**已删除**,日报改为活动时间线单步生成(见 §7);可选上云三挡已落地(§8)。L1 静止门参数已定稿、待实施;VLM(含原规划 L5)已整体移除(§2)。
关联:AI 日报管线(`src-tauri/src/ai/summary_*`)、Chat(`src-tauri/src/chat/`)、可选上云(`src-tauri/src/sync/engine/datasets.rs`)

---

## 1. 目标与约束

本设计统一了曾经分散的 AI 子系统(MobileNet 去重、OCR 规划、VLM 日报、Chat 规划——
前两者与 VLM 日报现已删除),面向**内置 Chat Bot** 这一最终消费端,回答如下类型的问题:

> 上周我在 Cursor 用了多久?我今天专注度如何?我什么时候购买的 XX?我下午在看什么视频?

硬约束:

1. **丢失有界**——信息完整性与算力成本权衡,不追求零丢失;可接受有界、可预期的丢失,但不容随机的永久漏。
2. **隐私红线**——截图数据默认永不出设备。唯一例外:用户显式开启的云端截图洞察
   (默认关 + 同意门 + 上传黑名单 + 仅用户自配端点,见 `docs/design/cloud-insight.md`)。

旧方案的失败模式(为什么推倒):

| 旧做法 | 失败点 |
|---|---|
| 每晚 VLM 描述去重后全部帧(~120 张/天) | 贵(GPU 十几分钟,CPU 不可用);没被抽中的帧信息永久丢失 |
| MobileNet(ImageNet 特征)语义去重 | 对文字瞎:同店铺不同商品页/同编辑器不同文件被误合并,需要标题守卫兜底 |
| 只上 OCR | 纯视觉页面(设计稿/照片/视频画面)必然漏 |
| 去重 = 删帧 | 必然丢信息;不去重则下游成本爆炸 |

---

## 2. 总览

```
截图(30s 间隔)+ 活动记录
        │
        ▼
┌─────────────────────────────────────────────────┐
│ 消化管线(进程内;批量默认 / 常驻 OCR 可选)         │
│                                                 │
│  L0 元数据    app/标题/时长          免费  100%全留 │
│  L1 静止门    帧差法(占比阈值)       ~1ms  拦~5-15% │
│  L2 文本层    OCR 逐字识别           ~0.5s  全量   │
│  L3 会话折叠  标题+行级并集去重        μs    真正的去重层│
└─────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────┐
│ memory.sqlite(屏幕记忆库,独立数据库)              │
│  帧记录 / 文本会话 + FTS / 行级留痕 / 聊天历史     │
└─────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────┐
│ Chat 大脑(tool-calling agent loop,已落地)        │
│  ├─ query_stats:主库统计(时长/占比类问题)         │
│  ├─ search_text:FTS 逐字搜索                     │
│  └─ get_timeline:时段会话时间线                  │
└─────────────────────────────────────────────────┘
        │
        ▼
  用户提问;日报·周报 = activities 活动时间线单步生成(见 §7)
```

核心思想:**级联漏斗**。每一级只把"上一级解释不了的帧"往下送;越贵的工具见到的帧越少,
且见到的必然值得。没有任何一级执行"删除"——只有"附着"(attach),
每帧的时间戳与归属永远可查。

**VLM 已移除**(2026-07-05 用户裁定,非延期):逐图描述管线(step1)、MobileNet
去重、云端视觉 API、原规划的 L5 理解层与 look_at_images 工具全部删除,产品内
不再有任何 VLM 调用点。纯视觉内容(视频/设计稿/游戏)的可回答边界 = L0 标题 +
时间("看了《××》多久"靠窗口标题即可);画面内容本身不做理解,为声明的已知边界。

---

## 3. 分级瀑布详解(L0-L3)

以典型日为例:8h 录制 × 30s 间隔 ≈ **960 帧**。

### L0 元数据(免费,100% 保留)

活动行(app、窗口标题、起止时间)本来就在主库。它是所有下游层的分组键,
也是隐私跳过时段(不截图的应用)的唯一信息来源——这类时段本来就只该有元数据。

### L1 静止门——帧差法(~1ms 级,拦 ~5-15%)

**定位:静止检测器,不是内容检测器**——只回答"这帧确定没变吗",拿不准一律放行。
内容级判断全部下放 L3(文本层,内容可见)。误放行的代价被 OCR 单价(0.13s)封顶,
全天全量误放行 ≈ 2 分钟计算;误附着则丢内容——两者代价不对称,故阈值偏保守(但按 §10 权衡,非零漏)。

**执行位置:抓拍管线内、JPEG 编码之前——附着的帧不写盘**(用户裁定)。
锚点帧的时间范围天然覆盖被附着的间隔;静止时段零新文件,磁盘/IO/retention churn 同步减免。

```
状态:锚点缩略(36KB/屏)+ 挂起帧(内存,≤1 帧,~0.3MB)
1. 新帧缩到 256×144 灰度,逐像素 |cur − anchor| > τ(=12)记变化,fraction=占比
2. fraction < f          → 附着:不写盘,锚点不动,记 dwell
3. fraction ≥ f          → 挂起(帧留内存,不落盘),等下一帧:
     下一帧 vs 锚点 < f   → 瞬态弹层(IME 候选框/通知横幅/菜单)→ 丢弃挂起帧
     下一帧 vs 锚点 ≥ f   → 持久变化确认 → 挂起帧补写盘(时间戳保留),锚点 := 该帧
4. 兜底阀:连续附着 K=4 帧 → 第 5 帧无条件走确认流程
   输出变化块掩码传给 L2(区域 OCR:裁条顺带解 Vision 相对图高地板)
```

- **持久性确认(一帧延迟)**:瞬态弹层(IME/通知/菜单)与真实编辑在变化面积上不可分,
  可分的是时间性——弹层撑不过一个采样间隔。越阈帧挂起一帧,下一帧回落即丢弃。
- **跟锚点比而非上一帧比**:打字类缓变相对锚点单调累积,必然越阈。
- **运营点**:f = 0.10% 单值,全分辨率通用(标定与召回数据见 §10 评测资产的脚本输出)。
  漏检方向安全:打字级小变化由 L3 行级并集吸收;最小面积原地替换由 K 阀封顶。
- **哈希族已证伪,勿回退**:网格均值/梯度类摘要(dHash 等)对同密度文本替换结构性
  失明,任何阈值无解。dHash16 仅保留为存储指纹(跨天近似查重),不守门。
- 已知盲区(均由 K 阀封顶):等亮度纯色变、低于 τ 的低对比变化、重复图案文本混叠
  (终端日志/表格滚动后像素对齐,丢的只是重复行内时间戳级差异)。

资源:帧差 ~1ms/帧,全天 ≈ 1-2s CPU,常驻 36KB/屏。
远期:插电常驻改持续捕获会话后,本层由合成器 dirty rects(SCK/DXGI)免费替代。

**配套的采集层改动:快门与时长记账解耦**

快门(截不截图)的活跃判据 = **屏幕在变**(L1 同款传感器);键鼠空闲只用于时长记账
(挂机封会话、不计时长)。两者不解耦,看视频/盯编译这类零输入时段会整段断供。

- 真挂机(屏幕静止):帧全部附着,不写盘,成本 ≈ 0;
- 被动观看(屏幕在变):帧照常进入 L2,并计入使用时长。

过渡实现已上线(`service.rs::screen_probe_active`,挂机判定第二信号,限频 20s、
截图功能关闭时停用);P1 时探测器并入 L1 静止门,同一传感器不留两套。

### L2 文本层——OCR 逐字识别(全量)

所有通过 L1 的帧全部 OCR,产出逐字文本行交 L3 折叠。**帧级识别结果不落库**——
全量重跑一天只要分钟级,比维护任何帧级缓存都便宜,折叠规则改了重跑即可。
成本:几百 ms/帧(CPU),全天数百帧 ≈ 分钟级,不需要抽样。

设计要点:**OCR 在去重之前**——先全录,再折叠。旧方案"先去重再分析"的信息丢失由此根除。

引擎(可替换抽象,一侧一实现):

- **Windows:PP-OCRv5(ONNX)**——单模型原生识别简中/繁中/英/日,混排页直接认,
  **无语言检测环节**;跑在既有 onnxruntime(嵌入模型同款)上,模型 ~20MB 走既有
  HF 下载管道;输出置信度 + 行框(系统 OCR 两者皆无)。小语种(韩文/西里尔等)
  = 换装对应识别模型文件,管线不动。
- **macOS:Vision**——原生多语言列表 + 自动检测;设 `minimumTextHeight = 0`
  (P2 合成图复测项)。

**输入分辨率结论(POC 实测)**:

- 唯一的地板:**原生渲染 ≲13px** 的细笔画识别不动,放大/插值救不回
  (16px 99%,13px 仅 27-33%)——由存档规格保原生像素应对。JPEG 压缩无损耗顾虑。
- **整图直喂**,不做任何预切分。
- **屏幕 DPI 决定生死**:2× Retina 屏 UI 文字 26px+ 物理,现有 1280 存档缩后是"高墨量 13px",
  99.7% 可读——**现状即可用**;1× 屏(QHD/1080p@100%)正文 14-16px 物理骑在地板上,
  **1280/q80 存档为不可逆毁灭**(实测存档图 225 乱码字符),历史档案无法回填。
- 一般人字号分布:界面 chrome 12-13px、正文 14-16px、代码 12-14(逻辑px)——
  1× 屏的 chrome 永久放弃(有 L0 标题兜底),正文靠原生像素存档挽救。
- **存档规格(已定案)**:`存档宽度 = 屏幕逻辑宽度(native ÷ scale),上限 2880;
  JPEG q85;Lanczos3 滤镜`(替换现状 1280/q80/Triangle)。
  - 1× 屏(QHD 2560)→ 存原生 2560,~300KB/张:14-16px 正文以原生锐利像素进 OCR;
  - 2× 屏(MacBook 2880)→ 存 1440,~180KB/张:26px 物理文字缩成"高墨量 13px",
    实测 99.7% 可读,且恰为逻辑分辨率、1:1 观看不糊;
  - 4K@100%(罕见)超上限截到 2880,小字接受损失并在文档声明。
  - 磁盘账:单张 2-4×,但 L1 静止帧不落盘抵消一部分,净增约 2-3×,retention 管上限。
  - 历史档案(1× 屏来源)无法回填文本,从新规格起累积。

### L3 会话折叠——标题定界 + 行级并集(μs 级,真正的去重层)

**去重的对象是帧,合并的是文字**:同一次阅读/编辑的 N 帧折叠成一条会话记忆,
文本取**行级并集**,一行不丢。这是全瀑布里唯一"看得见内容"的去重,
原地替换、大幅滚动等像素层盲区全部在此兜住。

```
会话边界 = (app, 标准化窗口标题) + 时间连续(间隔 ≤ 5min)
会话内每帧三态判定:
  ① 新行占比 ≈ 0(全部已在会话文本里)→ 附着(回看/静止)
  ② 有新行 → 并入:新行追加(记本帧时间戳=行级留痕),帧记为会话成员
  ③ 标题变 / 时间断裂 → 封会话,开新会话
```

- **simhash 降级为帧级预筛**(与上一帧文本几乎相同 → 跳过行级比对),
  **不再决定会话边界**——同窗口大幅滚动两视口文本几乎零重叠(实测真实案例:
  claude.ai 长对话滚动,86 行 + 58 行、仅 16 行重叠),按 simhash 切会话会把
  一次阅读碎成多条;按"选代表帧"去重则白丢 42 行独有内容。
- 行级留痕:每行记首次出现帧 → 搜索命中可给出精确到帧的证据卡。
- SPA 软分割(同标题内容全换):连续多帧与会话文本零重叠 → 会话内标记分割点,
  P2 打磨,不影响信息完整性,只影响时间线叙事粒度。
- 写码/文档/网页浏览等文字型工作 ~80% 的帧在本层终结;低文字会话(视频/设计稿/游戏)
  同样按"标题 + 时间连续"成会话——**嵌入聚簇已证伪,勿回退**(2026-07-05 真实数据:
  同一视频不同镜头的 CLIP 相似度仅 0.5-0.8,与"不同内容"区间重叠,任何簇半径无解,
  实测一个视频的 10 帧被 0.90 半径拆成 10 簇)。
- **MobileNet 语义去重已删除**(2026-07-05):它对"同标题滚动/替换"必然误合并
  (同版面 → 嵌入相似度 >0.95),且随 step1 下线后再无消费方;本层的行级并集
  是唯一的去重语义。

---

## 4. 信息丢失界

三档丢失,每档有明确的界:

| 档 | 什么帧 | 丢什么 | 保障 |
|---|---|---|---|
| 零丢失 | L1 静止帧(帧差<f=0.10%) | 无(像素未变;1440 档最小面积替换有 4% 漏检) | 持久性确认 + K=4 连附着强制落盘阀(≤2.5min) |
| 逐字保留 | L3 会话成员帧 | 版面装饰 | 行级并集一行不丢,FTS 可搜,行级留痕到帧 |
| 声明的边界 | 低文字会话的成员帧 | 画面内容(VLM 已移除,不做理解) | 标题 + 时间可定位;截图在 retention 内可人工回看 |

按信息类型看兜底通道:

| 信息类型 | 兜底通道 | 漏? |
|---|---|---|
| 屏幕文字(商品/聊天/代码/金额/字幕) | L2 OCR 逐字全量 | 不漏,逐字忠实 |
| 纯视觉内容(设计稿/照片/视频画面) | L0 标题 + 时间成会话可定位;画面内容不理解 | **已知边界**(VLM 已移除,用户裁定) |
| 被动观看时段(视频/编译,零键鼠输入) | 屏幕活跃探测保持 roll 补帧(已实施);P1 后由 L1 裁决 | 不漏(修复前:开场一帧后断供) |
| 被折叠/附着的帧 | 留痕表(`member → rep` + 时间戳) | 不漏,时间段可还原 |
| 截图被 retention 删除后 | 文本留在 memory.sqlite | 图没了字还在 |
| 隐私应用跳过截图的时段 | L0 活动行 | 只有画面,本该如此 |

---

## 5. 数据模型:memory.sqlite(独立数据库)

独立于主库(activities.sqlite)的第二个数据库文件。理由:
用户可独立删除/迁移这份资产;避开主库单连接(tokio_rusqlite)的写争用;
**默认不进云同步**(无 outbox;可选上云为显式三挡 opt-in,见 §8)。

实际 schema 已到 v3:`text_sessions` 增加 `guid`(跨设备全局 id)与 `origin_device`
(NULL=本机产出,非 NULL=同步自该设备);另有 Chat 会话历史两表
(`chat_conversations` / `chat_messages`,含 guid 与软删墓碑)。以下为设计基线:

```sql
-- 每个保留帧一条(通过 L1 的帧)
CREATE TABLE frames (
    path        TEXT PRIMARY KEY,   -- 截图路径(与主库对齐)
    ts          TEXT NOT NULL,      -- 拍摄时刻
    local_date  TEXT NOT NULL,
    app_id      TEXT, title TEXT,   -- L0 快照
    ocr_state   INTEGER NOT NULL DEFAULT 0,  -- 0待 1完 2失败
    attempts    INTEGER NOT NULL DEFAULT 0,  -- 失败重试计数(上限 3)
    session_id  INTEGER             -- L3 文本会话归属
);

-- L3 文本会话(折叠后的检索单元)
CREATE TABLE text_sessions (
    id INTEGER PRIMARY KEY,
    local_date TEXT NOT NULL,
    started_ts TEXT NOT NULL, ended_ts TEXT NOT NULL,
    app_id TEXT, title TEXT,
    simhash INTEGER,
    text TEXT NOT NULL              -- 会话合并文本(= session_lines 物化拼接)
);
CREATE VIRTUAL TABLE text_sessions_fts USING fts5(
    text, content='text_sessions', content_rowid='id',
    tokenize='trigram'              -- 中日文子串搜索,语言无关
);

-- L3 行级留痕:每个唯一行 + 首次出现帧(搜索命中 → 精确到帧的证据卡)
CREATE TABLE session_lines (
    session_id INTEGER NOT NULL,
    line_no    INTEGER NOT NULL,    -- 会话内序
    text       TEXT NOT NULL,       -- 标准化后 ≥6 字符
    first_path TEXT NOT NULL,       -- 首现帧
    first_ts   TEXT NOT NULL,
    PRIMARY KEY (session_id, line_no)
);

-- 附着留痕(L1 账本;迁移自主库 screenshot_dedup_map)
CREATE TABLE attach_map (
    member_path TEXT PRIMARY KEY,
    rep_path    TEXT NOT NULL,
    local_date  TEXT NOT NULL,
    created_at  TEXT NOT NULL
);
```


---

## 6. 消化 worker 生命周期

已落地形态(与原设计的差异:进程内任务而非独立子进程;子进程化时把内存互斥换文件锁即可):

- **形态**:进程内消化任务(`memory/digest.rs`)。OCR(PP-OCRv5)跑 onnxruntime
  (Windows 注册 DirectML,任何 DX12 GPU 可加速,失败自动回退 CPU)。
  内存 AtomicBool 单实例互斥;单帧失败标记重试(上限 3 次)后跳过,
  不让整批消化卡死在一帧上。
- **两档模式**(设置项,仅截图开启时可见):
  - **批量(默认)**:手动「立即回填/识别」触发,全速线程(核数-2,上限 16)清完积压即释放引擎;
  - **常驻 OCR(opt-in)**:引擎驻留内存(约 400MB),60s tick 准实时消化,保守线程(2-4);
- **失败语义**:任何一级失败只降级不阻塞——OCR 失败的帧仍有 L0/L1;`ocr_state=2` 可重试。

---

## 7. Chat 大脑(已落地,`src-tauri/src/chat/`)

guarded agent loop(护栏:步数上限/重复调用去重/降级阶梯/引用绑定),
四道墙防 LLM 篡改数据(①grammar 约束解码/原生 tools ②语义校验 ③固定参数化查询
④只读连接)。工具面(每类问题都有廉价通道):

| 工具 | 数据源 | 回答什么 |
|---|---|---|
| `query_stats` | 主库 SQL | 时长/占比/排行,支持应用与标题关键词过滤("Cursor 用了多久"/"《××》看了多久") |
| `search_text` | text_sessions_fts | 逐字内容("什么时候买的 XX") |
| `get_timeline` | text_sessions 按时间 | 某时段在干什么 |

回答携带**证据卡**:命中会话的时间段、应用、标题(行级留痕可给证据帧路径)。
双适配器:云端 OpenAI 原生 tools / 本地 llama-server grammar 约束解码;
对话模型独立槽位(`chat_main`),会话历史落 memory.sqlite。
(原规划的 `look_at_images` 随 VLM 移除一并取消。)

### 日报/周报(现状)

日报 = 单步纯文本管线(`summary_runner.rs`):activities 合成逐小时活动时间线
(应用时长 + 窗口标题样例)+ top apps → LLM 叙事,不经过 Chat 工具循环;
周报聚合日报文本。原 step1(夜跑 VLM 逐图描述)/交错 swap 管线已于 2026-07-05 删除。
"日报改为定时触发的模板化 Chat 查询"的完全并轨形态未采用——活动时间线直喂
更简单且质量已达标,除非后续出现只有工具循环才能满足的报告需求,不再计划并轨。

---

## 8. 隐私与生命周期

- 各级全部本地执行;云端 API(若配置)只承接**文本**(日报叙事/Chat 推理),
  **截图图片本体永不上传**(不可动摇的红线);
- **可选上云三挡**(2026-07-05 落地,云同步页,默认全关,开启弹琥珀警告):
  AI 总结文本 / 聊天历史 / 屏幕记忆全文,分别门控各自数据集的推与拉;
  数据只进用户自己的 Google Drive appData,每设备只写自己前缀的文件,
  guid + LWW 合并,删除靠软删墓碑传播;关闭开关即停,「清除云端数据」可删;
- retention 自动删除过期截图 → **只删图,记忆保留**(删图前保证已消化);
- 「忘记该设备」连带清掉从它同步来的屏幕记忆会话(origin_device 标记)。

---

## 9. 分期

| 期 | 内容 | 状态(2026-07-05) |
|---|---|---|
| **P1 记忆库** | memory.sqlite + 消化管线 L0/L2/L3(OCR/折叠/FTS)+ 常驻 OCR/回填 + 采集解耦 | **已落地**(L1 静止门欠账,见下) |
| **P2 Chat** | agent loop + 三工具 + 证据卡 + 独立页面 + 会话历史落库 + 对话模型槽位 + 隐私确认 | **已落地** |
| **P3 管线换代** | 旧日报管线(step1 VLM + MobileNet)删除;日报改活动时间线单步生成;提示词重写 | **已落地**(原"L5 + look_at_images + 报告并轨"方案随 VLM 移除取消) |
| **P4 上云** | 可选上云三挡(AI 总结/聊天历史/屏幕记忆全文,明示警告) | **已落地** |

剩余欠账:
- **L1 静止门**(参数已定稿,§10):抓拍管线内帧差附着,静止帧不落盘;
  实施时把 `screen_probe_active` 探测器并入,同一传感器不留两套;
- 月报(前端占位,后端未实现);
- SPA 软分割、标题标准化 v2 等 L3 打磨项。

历史遗留:`screenshot_dedup_map`(v35)与 `screenshot_embeddings` 表随 MobileNet
删除已无写入方,数据只读残留;attach_map 表待 L1 实施时创建。

---

## 10. 参数表(实测定案 + 待拍板)

**L1 静止门(已实测定案,`scripts/poc/l1-phash-poc.py --fd`)**

| 参数 | 值 | 依据 |
|---|---|---|
| 执行位置 | 抓拍管线内,编码前;**附着帧不写盘** | 用户裁定;静止时段零新文件 |
| 缩略尺寸 | 256×144 灰度 | 半个字/缩略像素,替换必可见;36KB/屏 |
| 像素阈值 τ | 12 / 255 | 滤 JPEG 纹波/字体平滑;真实变化 30-150 级 |
| 占比阈值 f | **0.10% 单值**(放宽,非 100% 召回) | 675/1080 档召回≈100%,1440 档最小正类 75-85% |
| 持久性确认 | 越阈帧挂起 1 帧,下一帧变化消失→丢弃 | IME/通知横幅等瞬态弹层 100% 拦截 |
| 兜底阀 K | 连续附着 4 帧强制走确认 | 漏检上确界 ≈ 2.5min 必落盘一帧 |
| 锚点语义 | 与上一保留帧比,附着不更新锚点 | 缓变累积必越阈 |
| 实测 | 抓拍路径 0.85ms/帧;真实流附着率 ~6% | 全天 ~1-2s CPU;挂起帧内存 ~0.3MB |

**L2 OCR(已实测定案)**

| 参数 | 值 | 依据 |
|---|---|---|
| 引擎 | Windows = PP-OCRv5(ONNX,复用 onnxruntime);mac = Vision | 单模型简中/繁中/英/日混排直认,无语言检测环节 |
| OCR 输入 | 存档整图直喂 | mac 需设 `minimumTextHeight=0`(P2 复测项) |
| 输入下限 | 原生渲染 ≥16px 物理文字 | 13px 即使无损也仅 27-33%;16px 为 99% |
| 识别质量评法 | 句子还原率(逐字地面真值) | 字符数/字多者为基准均已证伪 |
| 存档规格 | **宽 = 逻辑宽度(≤2880),q85,Lanczos3** | 1×屏原生锐利、2×屏高墨量缩放,两个 OCR 可用域 |
| 帧级结果 | 不落库,折叠后只存会话行 | 全量重跑分钟级,比维护帧级缓存便宜 |

**L3 会话折叠(设计定案,阈值待实装标定)**

| 参数 | 建议 | 说明 |
|---|---|---|
| 会话间隔上限 | 5 min | 同标题断流即封会话 |
| 行有效长度 | ≥6 字符(标准化后) | 滤图标/单字噪音 |
| simhash 预筛 | 距离 ≤3 跳过行级比对 | 仅加速,不定边界 |
| SPA 软分割 | 连续 3 帧与会话文本零重叠 | P2 |

全部参数落 `ai` 配置节;附着率/会话数等观测指标待 L1 实施时在调试页暴露。
评测资产:`scripts/poc/l1-phash-poc.py`(--pr 标注集评测 / --fd 帧差标定+资源基准)、
`scripts/poc/l1-dedup-eval.py`(真实截图流离线评测+全帧可视化报告,2026-07-05 首跑:
活跃开发日附着率 3.5%,机制全部按预期工作)、
`scripts/poc/l2-ocr-accuracy-poc.py`、`scripts/poc/ocr-cli.swift`;样张 `scripts/poc/output/l1|l2/`(已 gitignore)。
