# Rustfmt 项目偏好。`cargo fmt` 默认走的就是这套，CI 里 `cargo fmt --check` 也读这。
edition = "2021"
max_width = 100
hard_tabs = false
tab_spaces = 4
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
